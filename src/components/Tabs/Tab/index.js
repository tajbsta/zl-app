// `label` and `icon` props are used by the parent Tabs component
// but we should keep them here for text editor suggestions
// eslint-disable-next-line no-unused-vars
const Tab = ({ label, icon, children }) => children;

export default Tab;
